# 版本记录规范

## 1. 目的

这个文件专门规定：

`每次改完东西后，版本记录应该怎么写。`

目标很简单：

1. 让你自己能看懂
2. 让别的开发能接手
3. 让 AI 每次都按同一套规则写

从现在开始，版本记录不是“想写就写”，而是项目流程的一部分。

---

## 2. 什么时候必须更新版本记录

只要这次改动符合下面任意一条，就必须更新：

- 改了用户能看到的功能
- 改了页面样式或交互
- 改了价格、收益、快照、分析这些计算口径
- 改了登录、权限、版本更新、下载逻辑
- 改了部署、定时任务、线上运行逻辑
- 修了线上 bug
- 做了数据修复，并且会影响页面结果

可以不更新的情况：

- 纯注释修改
- 纯重命名，且没有行为变化
- 纯本地实验，最终没有进入正式代码
- 纯文档排版修正

---

## 3. 版本记录必须写在哪里

统一写到：

- [CHANGELOG.md](/Users/kona/Desktop/kaka/kona_repo/CHANGELOG.md)

这是项目版本历史的唯一标准记录。

不要再分散写到别的零碎文档里。

---

## 4. 版本记录和版本号要分开

必须明确：

- 写版本记录，不等于升级版本号
- 升级版本号，也不等于所有改动都要发新版

默认规则：

1. 重要改动写进 `CHANGELOG.md`
2. 只有准备发布新的客户端安装包时，才升级 Flutter 客户端版本号

通常需要升级版本号的情况：

- Flutter 客户端准备发新版
- 用户能感知到的客户端功能改动准备正式发布
- APK 下载、发版记录、版本检测链路都要进入新版本

通常不需要升级版本号的情况：

- 纯文档修改
- 目录整理
- 后端修复
- Web 修复
- 数据修复
- 本地联调的小改动
- 暂时不发新版安装包的 bug 修复

如果不确定要不要升级版本号，默认不要擅自改版本号，应先确认。

---

## 5. 固定模板

以后每个版本都必须按这个模板写：

```md
## v1.0.x

### 这版一句话

### 主要变化
- 
- 
- 

### 影响范围
- 
- 

### 验收重点
- 
- 
```

不允许随手换成一堆英文标题。

例如这些以后不再作为主结构使用：

- `Summary`
- `Added`
- `Changed`
- `Fixed`
- `Verification`

原因很简单：

这些写法更像给开发自己看的，不适合你现在的使用方式。

---

## 6. 写作规则

### 5.1 必须写大白话

版本记录默认写给“老板 / 产品 / 自己以后回看”。

所以要写成：

- 页面变好了什么
- 用户会感受到什么
- 风险在哪里

不要堆一堆黑话。

### 5.1.1 默认必须全中文

版本记录的默认语言规则是：

`全中文。`

除非用户明确要求，否则不要写：

- 英文标题
- 英文小节名
- 英文式 changelog 结构
- 中英混杂的技术话术

允许保留英文的范围，只限于：

- 代码文件名
- 命令
- 库名
- 接口名

而且出现后，应该立刻用中文解释。

### 5.2 先写结果，再写技术动作

要写：

`基金价格更准了`

不要先写：

`调整了价格源优先级`

技术动作不是不能写，但必须排在结果后面。

### 5.3 一定要写影响范围

每次都要告诉人：

- 这次影响了哪些页面
- 影响了哪些模块
- 会不会影响线上

### 5.4 一定要写验收重点

每次都要告诉人：

- 这版最该验什么
- 最容易出问题的地方在哪

### 5.5 没验就写没验

不要假装自己测过。

如果没跑真机、没跑构建、没上线上验证，就直接写：

`本次未做真机验收`

---

## 7. AI 必须遵守的规则

以后 AI 改代码时，只要改动影响：

- 功能
- 页面
- 数据口径
- 部署
- 线上行为

就必须同步更新 [CHANGELOG.md](/Users/kona/Desktop/kaka/kona_repo/CHANGELOG.md)。

而且必须遵守：

1. 使用本规范里的固定模板
2. 先写大白话结果
3. 不要堆难懂英文和技术术语
4. 没验收的内容不能假装验过
5. 如果用户已经明确说“验收通过”，版本记录必须在本轮收口阶段立即补写
6. 默认使用中文标题和中文条目，不要写成英文 release note 风格

### 6.1 用户验收通过后的默认动作

如果用户已经明确表示本轮修改验收通过，默认后续动作应该连起来执行：

1. 更新 `CHANGELOG.md`
2. 提交代码
3. 推送到 GitHub
4. 主动检查 GitHub Actions / CI
5. 如果流水线失败，优先自行修复并继续推送

除非遇到高风险、越权范围或明确阻塞，否则不要再反复找用户确认这些收口步骤。

---

## 8. 推荐写法示例

```md
## v1.0.32

### 这版一句话

这版主要修了基金价格显示不准和投资页收益口径不一致的问题。

### 主要变化
- 基金价格现在优先用更可信的数据
- 投资页累计盈亏和成本价显示更一致了
- 部分旧缓存不会再误导当天收益

### 影响范围
- 投资页
- 基金详情
- 后端价格接口

### 验收重点
- 重点验基金现价是否和外部页面一致
- 重点验累计盈亏和成本价是否对得上
```

---

## 9. 当前结论

从现在开始，版本记录的规则就是：

1. 改动影响产品行为，就必须写版本记录
2. 统一写进 `CHANGELOG.md`
3. 一律用大白话模板
4. 先写结果，再写技术
5. 不允许写自己都看不懂的记录
