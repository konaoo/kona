# 版本记录规范

## 1. 目的

这个文件专门规定：

`每次改完东西后，版本记录应该怎么写。`

目标很简单：

1. 让你自己能看懂
2. 让别的开发能接手
3. 让 AI 每次都按同一套规则写

从现在开始，版本记录不是“想写就写”，而是项目流程的一部分。

---

## 2. 什么时候必须更新版本记录

只要这次改动符合下面任意一条，就必须更新：

- 改了用户能看到的功能
- 改了页面样式或交互
- 改了价格、收益、快照、分析这些计算口径
- 改了登录、权限、版本更新、下载逻辑
- 改了部署、定时任务、线上运行逻辑
- 修了线上 bug
- 做了数据修复，并且会影响页面结果

可以不更新的情况：

- 纯注释修改
- 纯重命名，且没有行为变化
- 纯本地实验，最终没有进入正式代码
- 纯文档排版修正

---

## 3. 版本记录必须写在哪里

统一写到：

- [CHANGELOG.md](/Users/kona/Desktop/kaka/kona_repo/CHANGELOG.md)

这是项目近期变更记录的主入口。

不要再分散写到别的零碎文档里。

同时要明确：

- 根 [CHANGELOG.md](/Users/kona/Desktop/kaka/kona_repo/CHANGELOG.md) 只保留近期记录
- 更早的历史记录应归档到：
  - `archive/changelog/`

也就是说：

- `CHANGELOG.md` 是日常查看入口
- `archive/changelog/` 是历史归档区

不要把根 `CHANGELOG.md` 永远堆成一份无限增长的大文件。

---

## 4. 版本记录、Flutter 客户端版本、检查更新版本要彻底分开

必须明确，现在项目里有 3 套东西：

1. `CHANGELOG.md`
   这是变更记录。
   用来回答：
   - 这次改了什么
   - 会影响什么
   - 该重点验什么

2. `flutter/pubspec.yaml` 里的 `version`
   这是 Flutter 客户端版本。
   用来回答：
   - 当前准备发给用户安装的客户端版本是多少

3. 后端 `/api/app/version`
   这是 App 内“检查更新”接口返回的线上可下载安装版本。
   当前来源是：
   - `kona_tool/config.py` 里的 `CLIENT_APP_VERSION`
   - `CLIENT_APP_BUILD_NUMBER`

这三者默认不是一回事。

必须锁死下面这几条：

- 写版本记录，不等于升级 Flutter 客户端版本
- 升级 Flutter 客户端版本，不等于已经更新检查更新接口
- 检查更新接口版本不对齐，就会出现“App 明明有新版，但提示已是最新版本”

默认规则：

1. 重要改动写进 `CHANGELOG.md`
2. 只有准备发布新的客户端安装包时，才升级 Flutter 客户端版本号
3. 只有准备让 App 内“检查更新”提示新版本时，才更新 `/api/app/version` 对应的版本配置

通常需要升级 Flutter 客户端版本号的情况：

- Flutter 客户端准备发新版
- 用户能感知到的客户端功能改动准备正式发布
- APK / IPA 下载、发版记录、版本检测链路都要进入新版本

通常不需要升级 Flutter 客户端版本号的情况：

- 纯文档修改
- 目录整理
- 后端修复
- Web 修复
- 数据修复
- 本地联调的小改动
- 暂时不发新版安装包的 bug 修复

通常需要同步更新“检查更新接口版本”的情况：

- 新的 Flutter 安装包已经准备发布
- 用户点击“检查更新”应该能看到新版本
- 下载链接、版本号、构建号、更新文案已经准备好

通常不需要更新“检查更新接口版本”的情况：

- 只是本地开发
- 只是 Web / 后端 / 管理后台改动
- Flutter 改了，但还没真的出新包
- 只是改了 `CHANGELOG.md`

如果不确定要不要升级版本号，默认不要擅自改 Flutter 版本号，也不要擅自改检查更新接口版本，应先确认。

---

## 5. 以后 `CHANGELOG` 不再冒充客户端版本号

历史上 `CHANGELOG.md` 里出现过很多 `v1.0.x` 条目。

这些历史条目先保留，不回头大重写。

但从现在开始，必须明确：

- `CHANGELOG` 记录的是项目变更
- 不是 Flutter 客户端真实发版版本
- 不是 App 内“检查更新”接口版本

以后新条目推荐改成这种格式：

```md
## 2026-03-08-01
```

也就是：

- `日期`
- `当天第几次正式记录`

这样好处很直接：

1. 不会再和客户端版本号混淆
2. 不会再让 AI 把每次小改都写成“又发了一个 1.0.x”
3. 一眼就能看出这是内部变更记录，不是安装包版本

以后默认原则是：

- `CHANGELOG` 条目编号优先使用 `日期-序号`
- Flutter 客户端发版继续使用真实客户端版本号
- App 内检查更新继续使用线上可下载安装版本号

三者不要继续混写。

例如：

- 开发变更记录：`2026-03-28-01`
- Flutter 安装包版本：`1.0.29`
- 检查更新接口版本：`1.0.29`

---

## 6. 固定模板

以后每个版本都必须按这个模板写：

```md
## 2026-03-08-01

### 这版一句话

### 主要变化
- 
- 
- 

### 影响范围
- 
- 

### 验收重点
- 
- 
```

不允许随手换成一堆英文标题。

例如这些以后不再作为主结构使用：

- `Summary`
- `Added`
- `Changed`
- `Fixed`
- `Verification`

原因很简单：

这些写法更像给开发自己看的，不适合你现在的使用方式。

---

## 6.1 根 `CHANGELOG.md` 保留范围

根 [CHANGELOG.md](/Users/kona/Desktop/kaka/kona_repo/CHANGELOG.md) 默认只保留：

1. 最近一段时间的正式变更记录
2. 当前仍然经常需要回看的重要阶段记录

默认不建议继续把所有历史都永远堆在根文件里。

推荐做法：

1. 根 `CHANGELOG.md` 保留近期记录
2. 更早的记录按阶段或时间段归档到 `archive/changelog/`

推荐归档命名：

- `archive/changelog/2026-03.md`
- `archive/changelog/2026-Q1.md`

默认原则：

`主文件负责好看和好查，归档文件负责保历史。`

---

## 7. 什么情况下更新 `CHANGELOG`，什么情况下更新 Flutter version

以后 AI 默认按这张表执行：

| 场景 | 更新 `CHANGELOG` | 更新 Flutter version | 更新检查更新接口版本 |
| --- | --- | --- | --- |
| 改 Web 页面 / 交互 | 是 | 否 | 否 |
| 改后端接口 / 口径 | 是 | 否 | 否 |
| 改管理后台 | 是 | 否 | 否 |
| 改线上数据 / 配置 / 定时任务 | 是 | 否 | 否 |
| 改 Flutter，但还没准备发包 | 是 | 否 | 否 |
| 改 Flutter，并准备正式发新包 | 是 | 是 | 是 |
| 只改文档排版 / 注释 | 否（通常） | 否 | 否 |

一句话：

- `CHANGELOG` 是改动记录
- `Flutter version` 是客户端发版版本
- `检查更新接口版本` 是线上告诉用户“现在能下载哪个版本”

这三者以后不要再混着改。

---

## 8. 写作规则

### 8.1 必须写大白话

版本记录默认写给“老板 / 产品 / 自己以后回看”。

所以要写成：

- 页面变好了什么
- 用户会感受到什么
- 风险在哪里

不要堆一堆黑话。

### 8.1.1 默认必须全中文

版本记录的默认语言规则是：

`全中文。`

除非用户明确要求，否则不要写：

- 英文标题
- 英文小节名
- 英文式 changelog 结构
- 中英混杂的技术话术

允许保留英文的范围，只限于：

- 代码文件名
- 命令
- 库名
- 接口名

而且出现后，应该立刻用中文解释。

### 8.2 先写结果，再写技术动作

要写：

`基金价格更准了`

不要先写：

`调整了价格源优先级`

技术动作不是不能写，但必须排在结果后面。

### 8.3 一定要写影响范围

每次都要告诉人：

- 这次影响了哪些页面
- 影响了哪些模块
- 会不会影响线上

### 8.4 一定要写验收重点

每次都要告诉人：

- 这版最该验什么
- 最容易出问题的地方在哪

### 8.5 没验就写没验

不要假装自己测过。

如果没跑真机、没跑构建、没上线上验证，就直接写：

`本次未做真机验收`

---

## 9. AI 必须遵守的规则

以后 AI 改代码时，只要改动影响：

- 功能
- 页面
- 数据口径
- 部署
- 线上行为

就必须同步更新 [CHANGELOG.md](/Users/kona/Desktop/kaka/kona_repo/CHANGELOG.md)。

而且必须遵守：

1. 使用本规范里的固定模板
2. 先写大白话结果
3. 不要堆难懂英文和技术术语
4. 没验收的内容不能假装验过
5. 如果用户已经明确说“验收通过”，版本记录必须在本轮收口阶段立即补写
6. 默认使用中文标题和中文条目，不要写成英文 release note 风格

### 9.1 用户验收通过后的默认动作

如果用户已经明确表示本轮修改验收通过，默认后续动作应该连起来执行：

1. 更新 `CHANGELOG.md`
2. 提交代码
3. 推送到 GitHub
4. 主动检查 GitHub Actions / CI
5. 如果流水线失败，优先自行修复并继续推送

除非遇到高风险、越权范围或明确阻塞，否则不要再反复找用户确认这些收口步骤。

---

## 10. 推荐写法示例

```md
## 2026-03-08-01

### 这版一句话

这版主要修了基金价格显示不准和投资页收益口径不一致的问题。

### 主要变化
- 基金价格现在优先用更可信的数据
- 投资页累计盈亏和成本价显示更一致了
- 部分旧缓存不会再误导当天收益

### 影响范围
- 投资页
- 基金详情
- 后端价格接口

### 验收重点
- 重点验基金现价是否和外部页面一致
- 重点验累计盈亏和成本价是否对得上
```

---

## 11. 当前结论

从现在开始，版本记录的规则就是：

1. 改动影响产品行为，就必须写版本记录
2. 统一写进 `CHANGELOG.md`
3. `CHANGELOG`、Flutter 客户端版本、检查更新接口版本必须分开维护
4. 一律用大白话模板
5. 先写结果，再写技术
6. 不允许写自己都看不懂的记录
