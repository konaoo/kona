# kona_tool/scripts 目录说明

`kona_tool/scripts/` 放的是后端专属脚本，不是仓库级脚本。

这里适合放：

- 定时任务入口
- 数据修复脚本
- 运维辅助脚本
- 后端专属的一次性迁移脚本
- 本地开发库初始化脚本

这里不适合放：

- 纯临时排障脚本
- 跑完就没价值的输出文件
- 和后端主工程无关的试验文件

判断标准很简单：

如果脚本只服务 `kona_tool`，就放这里。
如果脚本服务整个仓库，应该放到仓库级 `scripts/`。

当前新增：

- `setup_local_demo_data.py`
  用途：重建本地固定测试账号 `konae / qq111111`，并补本地联调常用样例数据。
  范围：只动本地开发库，不碰线上。
