# kona_tool/tests 目录说明

`kona_tool/tests/` 放的是后端正式测试。

这里应该放：

- 单元测试
- 接口测试
- 回归测试
- 针对价格源、快照、分析口径的校验测试

这里不应该放：

- 手工验证脚本
- 临时排障脚本
- 跑完一次就没用的输出文件

判断标准：

如果文件可以被 `python -m unittest` 或正式测试流程反复执行，它才适合留在这里。
