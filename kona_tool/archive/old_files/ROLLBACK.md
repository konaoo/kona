# 快速回退指南

## 稳定版本: v11.1.1

**功能状态：**
- ✅ 完整的前端模板系统重构
- ✅ 统一的侧边栏和导航
- ✅ 独立的投资页面（investment.html）
- ✅ 专门的资产分析页面（analysis.html）
- ✅ 优化的资产页面（assets.html）
- ✅ 改进的代码组织和可维护性
- ✅ 更好的错误处理

**之前的稳定版本：v10.9.10**
- ✅ 数据正常显示（全部21条）
- ✅ 分类切换正常（全部、A股、基金、港股、美股）
- ✅ 无感知数据更新（只更新变化的数值，不闪烁）
- ✅ 港股、美股、基金价格正常获取

## 快速回退命令

### 方法1：使用最新稳定标签（推荐）
```bash
git checkout stable-v11.1.1
```

### 方法2：使用版本标签
```bash
git checkout v11.1.1
```

### 方法3：回退到之前的稳定版本
```bash
git checkout stable-v10.9.10
```

## 重启服务
```bash
# 停止当前服务
lsof -ti:5003 | xargs kill -9

# 重启Flask
python3 app.py &
```

## 查看所有版本标签
```bash
git tag -l
```

## 查看版本记录
```bash
cat CHANGELOG.md
```

## 当前已验证的版本
- **v11.1.1** - 最新稳定版本（模板系统重构，前端架构优化）
- v10.9.10 - 之前的稳定工作版本（数据正常显示，分类切换正常）
- v10.9.10 - 修复：使用绝对URL避免浏览器相对URL拼接问题

## 如果需要继续在main分支开发
回退到稳定版本后，如果需要继续开发：
```bash
# 创建新的分支进行开发（基于最新稳定版本）
git checkout -b feature-xxx stable-v11.1.1

# 或基于之前的稳定版本
git checkout -b feature-xxx stable-v10.9.10
```
