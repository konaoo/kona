# 环境变量配置模板
# 复制此文件为 .env 并填写实际值

# JWT 认证密钥（必须设置，用于用户登录加密）
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-super-secret-jwt-key-change-this

# 数据库路径（可选，默认 portfolio.db）
# KONA_DATABASE_PATH=/home/ec2-user/portfolio/kona_tool/portfolio.db

# 服务器配置（可选）
# HOST=0.0.0.0
# PORT=5003
# DEBUG=False

# 日志级别（可选，默认 INFO）
# LOG_LEVEL=INFO

# SMTP 邮件配置（验证码）
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=AKIA...
# SMTP_PASS=xxxx
# SMTP_FROM=konaeee@gmail.com
# SMTP_FROM_NAME=Kaka
# SMTP_USE_TLS=true

# 告警接收邮箱（逗号分隔；为空时默认发到 SMTP_FROM）
# ALERT_NOTIFY_TO=konaeee@gmail.com
# 可选：健康检查地址
# KONA_HEALTH_URL=http://127.0.0.1:5003/health

# 免验证码白名单（逗号分隔）
# LOGIN_BYPASS_EMAILS=konaeee@gmail.com

# 限流存储后端（生产建议 Redis）
# 本地开发可保留默认 memory://
# RATELIMIT_STORAGE_URL=redis://127.0.0.1:6379/0

# 行情运行指标接口（/api/system/price_health）访问令牌
# 为空表示不鉴权；生产建议设置高强度随机值
# PRICE_HEALTH_TOKEN=replace-with-strong-token

# 快照后台开关（推荐都为 false，使用 systemd timer）
# ENABLE_BACKGROUND_SNAPSHOT=false
# ENABLE_STARTUP_SNAPSHOT=false
